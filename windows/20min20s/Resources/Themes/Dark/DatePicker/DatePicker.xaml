<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Project1.UI.Controls.DatePicker;assembly=Project1.UI"
                    xmlns:converter="clr-namespace:Project1.UI.Controls.Converters;assembly=Project1.UI"
                    >
    <converter:StringConverter x:Key="StringConverter"/>
    <Style TargetType="{x:Type controls:DatePicker}">
        <Setter Property="Background" Value="#181825" />
        <Setter Property="BorderBrush" Value="#45475A" />
        <Setter Property="Width" Value="156" />
        <Setter Property="Height" Value="42" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:DatePicker}">
                    <Grid>
                        <Popup
                            x:Name="Popup"
                            Placement="Top"
                            PlacementTarget="{Binding ElementName=Border}"
                            AllowsTransparency="True"
                            HorizontalOffset="0"
                            >
                            <Border
                                x:Name="PopupBorder"
                                Background="#1E1E2E"
                                BorderBrush="#313244"
                                BorderThickness="1"
                                Padding="14"
                                Margin="15"
                                CornerRadius="16"
                                >
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="18" Opacity="0.3" ShadowDepth="0" Color="#05070B"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid>
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource Lang_Year}" FontWeight="SemiBold" FontSize="13" Foreground="#A6ADC8"/>
                                            <controls:ItemList x:Name="YearsList" Margin="0,5,0,0"/>
                                        </StackPanel>

                                    </Grid>
                                    <Grid Grid.Column="1">
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource Lang_Monthly}" FontWeight="SemiBold" FontSize="13" Foreground="#A6ADC8"/>
                                            <controls:ItemList x:Name="MonthsList" Margin="0,5,0,0"/>
                                        </StackPanel>

                                    </Grid>
                                    <!--<Path 
                      Width="15"
                      Height="15"
                      SnapsToDevicePixels="False"
                      Data="M 0 0 L 7.5 7.5 L 15 0" Fill="White"
                                        Stroke="#ccc"
                                        StrokeThickness="1"
                                        VerticalAlignment="Bottom"
                                        HorizontalAlignment="Left"
                                        Margin="-5,0,0,-25"
                                        />-->
                                </Grid>

                            </Border>
                        </Popup>
                        <Border
                            x:Name="Border"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"
                            CornerRadius="12"
                            Padding="12,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock FontFamily="/Project1.UI;component/Assets/IconFonts/#Fabric External MDL2 Assets" Text="&#xEE93;" VerticalAlignment="Center"  FontSize="14" TextAlignment="Center" Foreground="#CBA6F7"/>
                                <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" Grid.Column="1" Foreground="#A6ADC8" Text="{DynamicResource Lang_Monthly}"/>
                                <TextBlock Margin="8,0,0,0" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" Grid.Column="1" Foreground="#CDD6F4">
                                <Run Text="{TemplateBinding SelectedYear,Converter={StaticResource StringConverter}}"/> <Run Text="/"/> <Run Text="{TemplateBinding SelectedMonth,Converter={StaticResource StringConverter}}"/>
                                </TextBlock>
                            </StackPanel>

                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Popup" Property="IsOpen" Value="true"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="#585B70"/>
                            <Setter TargetName="Border" Property="Background" Value="#1E1E2E"/>
                        </MultiTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>


</ResourceDictionary>
